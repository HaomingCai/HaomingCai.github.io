_site
.env
.env.*
.DS_Store
.jekyll
.jekyll-metadata
.bundle
.sass-cache
# Gemfile / Gemfile.lock 原先在这里被忽略 —— 后果是 clone 下来的人（包括换了新机器的
# 自己）执行 MAINTENANCE.md 里的 `bundle exec jekyll serve` 时，根本没有东西可以 resolve，
# 本地压根跑不起来。现在提交它们，构建环境才是可复现的。
# 注意 _config.yml 的 exclude 里仍然列着这两个文件 —— 那是"不要发布到站点上"，
# 和"要不要进版本库"是两件事，别混淆。
node_modules
package.json
favicon.ico
*vendor*

# 设计迭代快照：203 张截图 / 357MB，是"让下一轮能对比上一轮"用的过程留痕。
# 定稿之后它们的使命就结束了 —— 留在磁盘上自己看，但不进 git。
# 计划 / 评分 / 快照脚本这三个小文件记录了决策，留着。
design_versions/*
!design_versions/v10_PLAN.md
!design_versions/SCORES.md
!design_versions/snap.sh

__pycache__/
*.pyc
