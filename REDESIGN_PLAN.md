# 个人主页重构计划

> **目标**：把现在这个"能用但不好看"的学术主页，重构成一个有审美、有个性、能让人记住的页面。
> **方法**：用 `/loop` 自主迭代多个版本，每轮**真实截图 + 自我打分 + 对抗式评审**，直到收敛。

---

## 0. 一句话目标

做一个**编辑式极简（editorial minimal）**的学术主页 —— 米白暖底、衬线体大标题、大量留白、克制的强调色。气质是**优雅、克制、有品味**，而不是"极客、酷炫"。

判断标准很朴素：**如果这个页面出现在 Hacker News 或 Twitter 上，会不会有人说"这网站真好看"？**

---

## ⚠️ 重要：方向已在设计阶段修正过一次

**原本你在选择题里选的是"暗色实验室风 + 深色为主"。但我把你给的 8 个参考站全部截图 + 采样了背景色，发现 7 个是浅色的** —— 你收藏的站和你勾的选项互相矛盾。

| 站点 | 实际背景色 | 明/暗 |
|---|---|---|
| ceciliavision | `rgb(247,244,242)` 米白 | ☀️ |
| mengweiren | `rgb(250,250,249)` 米白 | ☀️ |
| qianqianwang | 白 | ☀️ |
| haoyuchen | `rgb(255,255,255)` 纯白 | ☀️ |
| tx-leo | `rgb(244,244,244)` 浅灰 | ☀️ |
| openai | 白 | ☀️ |
| anthropic | `rgb(250,249,245)` 奶油 | ☀️ |
| ilyac | `rgb(0,0,0)` **纯黑** | 🌙 |

唯一的深色站 ilyac 用的是**纯黑 + Arial + 橙红链接** —— 气质跟你现在这个站高度相似，而你不满意它。

**结论：你的真实品味是浅色、暖调、衬线点缀、编辑式极简。方向已据此修正，你已确认。**

---

## 1. 现状诊断（基于真实渲染截图）

| # | 问题 | 具体表现 |
|---|------|---------|
| 1 | **字号层级几乎不存在** | 正文、作者、venue、摘要全是 14px。信息没有主次 |
| 2 | **强调色多达 3 种且不协调** | 蓝 `#4a9eff` + 黄 `#f8bb03` + 橙 `#f09228`。像三个人分别做的 |
| 3 | **缩略图尺寸完全不统一** | 有的高、有的宽、有的方。左边缘参差不齐，整列没有节奏 |
| 4 | **纯黑底 + 纯白字 = 廉价感** | `#000` 配 `#fff` 对比过硬。（现已整体转向浅色，此问题从根上消失） |
| 5 | **"Let's Chat!" 蓝框太重** | 填充色块横在页面上部，视觉重量远超信息价值 |
| 6 | **垂直节奏混乱** | 区块间距忽大忽小，条目内部又挤得慌 |
| 7 | **头像位置尴尬** | 右上角一张小图，和左边 bio 没有构图关系 |
| 8 | **`default.html` 有 4 段重复 Liquid** | 四个分区各复制一遍，改一处要改四处 |
| 9 | **视频 JS 过度工程** | 200 行 setInterval 轮询 + console.log 满天飞，为解决 `loop` 属性就能解决的问题 |
| 10 | **ClustrMaps 访客地图** | 拖慢加载，视觉上很"2010 年代个人网站" |

---

## 2. 已锁定的决定（硬约束，loop 不得违背）

### 视觉方向
- **风格**：编辑式极简（editorial minimal）
- **配色**：**浅色为主**。暖调米白/奶油底（如 `#faf9f5` / `#f7f4f2`），**不许用纯白 `#fff`**
- **文字**：暖调深灰（如 `#1a1a1a` / `#2a2a2e`），**不许用纯黑 `#000`**
- **强调色**：**未定，由 loop 探索**（见第 5 节 v1–v3）。最终只保留 **1 个主强调色**

### 字体（三级分工 —— 这是气质的核心）
| 用途 | 字体类型 | 参考 |
|---|---|---|
| 大标题（你的名字、分区标题） | **衬线体** | Cormorant Garamond / Source Serif / Newsreader |
| 正文（bio、摘要、作者列表） | **无衬线体** | Inter / Geist |
| 元信息（日期、venue、链接小字） | **等宽体** | JetBrains Mono / IBM Plex Mono |

- ❌ 不许全站等宽 —— bio 和作者列表会没法读
- ❌ 不许全站衬线 —— 长列表会疲劳

### 结构
- **单页**，所有内容在首页（bio → news → publications → 猫）
- ❌ 不拆分 `/publications` 独立页

### 内容取舍
| 内容 | 决定 |
|------|------|
| 论文缩略图（gif/视频） | ✅ **保留，但克制** —— 统一比例、去边框阴影、视频低调播放不抢戏 |
| News 时间线 | ✅ 保留 |
| Chief Vibe Officer 猫咪画廊 | ✅ 保留，页面底部，低调优雅 |
| "Let's Chat!" 蓝框区块 | ❌ **删除区块**，"欢迎约聊"用一行低调文字融进 bio |
| ClustrMaps 访客地图 | ❌ **删除**，替代方案见第 7 节 |

### 技术
- **保留 Jekyll**。`_posts/*.markdown` 和 `_data/authors.yml` 的数据结构**一个字都不许改**
  - 理由：以后加论文只需新建一个 markdown 文件，这个便利性不能丢
- 重写范围：`_layouts/default.html`、`style.scss`、`_sass/*`
- **只用原生 JS，不上任何框架**
- 页面必须**无 JS 也能正常阅读**（JS 只做增强，不做骨架）
- **消灭那 4 段重复 Liquid** —— 用一个 `_includes/publication.html` 复用

---

## 3. 参考网页与设计 DNA（已抓取分析）

### 你给的站
| 站点 | 值得偷的东西 |
|---|---|
| **[mengweiren.com](https://www.mengweiren.com/)** ⭐ **首要基准** | Cormorant Garamond 衬线大标题 + Inter 正文的对比；圆形头像居中；巨大的垂直留白；正文行高极舒服 |
| **[anthropic.com](https://www.anthropic.com/)** | 奶油底 `#faf9f5` 的暖度；Serif 标题 + Sans 正文的分工 |
| **[ceciliavision.github.io](https://ceciliavision.github.io/)** | 米白底；小型大写字母（small caps）做分区标题 |
| **[haoyuchen.com](https://haoyuchen.com/)** | Geist 字体的现代感；干净的论文卡片 |
| **[qianqianwang68.github.io](https://qianqianwang68.github.io/)** | 经典学术布局的信息密度控制 |
| **[tx-leo.github.io](https://tx-leo.github.io/)** | 浅灰底的柔和感 |
| **[openai.com](https://openai.com/)** | 极简导航；克制的动效 |
| **[ilyac.info](https://ilyac.info/)** | ⚠️ 唯一深色站。**内容组织**（About/Contact/Professional/Unprofessional 的标签式 bio）值得学，**配色和字体不学** |

### 提炼出的 3 条核心手法
1. **字体三级分工**（衬线标题 / 无衬线正文 / 等宽元信息）—— 这是"有品味"最廉价的实现方式
2. **暖调而非纯白**（`#faf9f5` 而非 `#ffffff`）—— 一个色值的差别，但质感天壤之别
3. **留白比内容更重要** —— mengweiren 的头图区留白高度接近一屏，敢于留空是成熟的标志

### 补充基准（我会自己抓）
Siggraph 一作 / graphics 方向学者主页（用户建议），作为额外参考。

---

## 4. 评分标准（loop 的自我判断依据 = "how to verify"）

每轮产出完整版本后：**截图（桌面 1280px + 手机 390px）→ 真的看图 → 按 6 维度打分**。

| 维度 | 满分 | 具体在看什么 |
|------|-----|-------------|
| **排版层级** | 20 | 三级字体分工是否到位？一眼能否分辨标题/作者/venue/摘要？行高字距是否舒服？ |
| **配色协调** | 20 | 强调色是否只有 1 个主色？底色是否有暖度（非纯白）？对比度是否护眼？ |
| **垂直节奏** | 15 | 间距是否遵循统一系统（8px 基准）？**是否敢于留白**？扫读有呼吸感吗？ |
| **视觉一致性** | 15 | 缩略图比例统一？左边缘对齐？hover 效果全站一致？ |
| **简洁度** | 15 | 有无多余边框/阴影/圆角/装饰？能删的都删了吗？ |
| **技术质量** | 15 | HTML 语义化？无 JS 可读？重复 Liquid 消除了吗？ |
| **合计** | **100** | |

### 通过线与停止条件
- **单项不得低于 12/20 或 9/15**（防止某维度拖后腿却靠其他项凑分）
- **总分 ≥ 88** 才算可交付
- **收敛判定**：连续 2 轮总分提升 < 3 分 → 停止
- **硬上限**：最多 10 轮

### 每轮必做的客观检查
```
✓ Jekyll 构建无报错
✓ 桌面 + 手机截图，肉眼过一遍
✓ 手机 390px 下无横向滚动条
✓ 所有论文缩略图渲染成功（无破图）
✓ 作者超链接功能正常（点 Jinjin Gu 能跳走）
✓ 对比度符合 WCAG AA（正文 ≥ 4.5:1）
```

### 🛡️ 防自欺的两道保险（关键）

**自己给自己的审美打分本质上不可靠** —— 模型倾向于给自己的作品打高分，收敛到"干净但平庸"的局部最优。两道保险：

1. **每轮必须和 baseline 并排对比**，不只跟上一轮比 —— 防止温水煮青蛙式的缓慢退化
2. **收敛后强制跑一轮"毒舌评审"**：spawn 一个**全新的 agent**，不告诉它这是我做的，只给它截图 + 参考站截图，让它以"刻薄的设计师"身份挑刺。它没有维护自己作品的动机，会诚实得多。
   - 如果毒舌评审提出了有效批评 → **必须再迭代**，不许直接交付

---

## 5. Loop 怎么跑

### 版本管理
- 每轮成果快照到 `design_versions/vN/`（含 `desktop.png` + `mobile.png`）
- 评分和改动理由记在 `design_versions/SCORES.md`
- **好处：跑完可以把所有版本截图并排看，随时说"我要 v3 那个配色"**

### 每一轮的动作
```
1. 读 REDESIGN_PLAN.md + SCORES.md（历史评分）
2. 决定这轮改什么（针对上轮扣分最多的维度）
3. 改 _layouts/default.html + _includes/ + style.scss
4. Jekyll 构建 → 起本地服务 → Playwright 截图
5. 肉眼看截图 → 对比 baseline + 上一轮 → 按 6 维度打分 → 写进 SCORES.md
6. 判断收敛 → 收敛则跑毒舌评审 → 有效批评则继续迭代
```

### 阶段 A：强调色探索（v1–v3，约 40 分钟）
底色统一为暖米白，**只变强调色和标题字体**，做出明确差异：

- **v1 — 墨蓝**：`#2c4a7c` 系。最学术、最安全、最耐看
- **v2 — 赤陶/铁锈**：`#b5533c` 系。暖色点缀，呼应 anthropic 的暖调，个性强
- **v3 — 苔绿/橄榄**：`#4a5d3a` 系。冷静内敛，graphics 圈常见

### 🔔 阶段 A 结束：**强制检查点**（推送到手机）

**v3 跑完后 loop 会停下，用 `PushNotification` 把三张截图推到你手机**，等你回一句"选 v2"之类的再继续。

> **⚠️ 这个检查点依赖你先做两件事，否则推送到不了你手机，loop 会干等一整晚：**
> 1. 在会话里输入 `/remote-control`（用手机 Claude App 扫码接上）
> 2. 输入 `/config`，打开 **"Push when Claude decides"** 和 **"Push when actions required"**
>
> 电脑必须保持开机、进程不关、不长时间断网（离线 >10 分钟会话会超时退出）。**记得关掉自动睡眠。**

### 阶段 B：选定方向后深耕（v4–v8 左右，约 1–1.5 小时）
在你选的配色上迭代，针对扣分最多的维度改，直到收敛（连续 2 轮提升 < 3 分）。

### 阶段 C：毒舌评审 + 修正（1–2 轮）
收敛后 spawn 全新 agent 挑刺；有有效批评则继续迭代。

### 📊 今晚预期产出：**约 7–9 个版本**
- 阶段 A：3 个（三种配色）
- 阶段 B：3–5 个（深耕迭代）
- 阶段 C：1–2 个（毒舌评审后的修正）

**总耗时约 2 小时，不是一整晚。** 收敛即停 —— 强行让它跑通宵只会导致负优化（为了让分数变化而瞎改，把已经好看的东西改坏）。

### 你的介入点
- **随时可以打断**
- **v3 后必须回一句**（选配色）—— 会推到你手机
- 觉得跑歪了，直接说"回到 v2 的方向"

---

## 6. 安全边界

### 🔒 Git 远端已移除（物理隔离）

这个目录是你的**本地副本**，但它原本的 git remote **直接指向线上真站** `git@github.com:HaomingCai/HaomingCai.github.io.git`。

**处理：已执行 `git remote remove origin`。** 现在 remote 列表为空 —— push 在物理上不可能发生，而不只是"规则上禁止"（规则会被违反，物理限制不会）。

- **保留 `.git` 本身**：它是这次 loop 最重要的安全网 —— 每轮快照、随时回滚、和 baseline 对比 diff、"我要 v3 那版"全靠它。删掉 `.git` 反而让 loop 变危险。
- **想恢复远端**（确认满意、准备上线时）：
  ```
  git remote add origin git@github.com:HaomingCai/HaomingCai.github.io.git
  ```

### loop 自主跑的时候绝对不能

- ❌ 改 `_posts/` 里任何 markdown 的内容（论文数据是资产）
- ❌ 改 `_data/authors.yml`
- ❌ 删除或压缩 `tn/` 里任何图片/视频原始素材
- ❌ 重新添加 git remote 或尝试 push
- ❌ 改 `CNAME`（会搞挂域名）
- ❌ 引入任何需要付费或 API key 的服务

**每轮开始前先 `git status` 检查**，确保没有未提交的工作被覆盖。

### 开跑前的清理项
仓库当前有 **1057 个未提交的删除**，已核实**全部**是 `tn/projects_html/vis2bokeh/` 的旧副本（上个 commit "把 vid2bokeh 移到根目录" 的残留），**没有一个是意外删除**。

loop 开始前需要先 commit 掉，否则会和设计改动混在同一批 diff 里，事后没法区分。

---

## 7. 访客统计替代方案（loop 结束后再配，今天不用动手）

ClustrMaps 会被删掉。

| 方案 | 优点 | 缺点 |
|------|------|------|
| **GoatCounter**（推荐） | 免费、开源、隐私友好（无 cookie）、脚本仅 3KB、后台干净 | 需注册（免费） |
| **Plausible** | 界面最漂亮 | **要钱**（$9/月起） |
| **Umami**（自托管） | 数据完全自控 | 要自己部署服务器 |
| **保留现有 GA** | 已在跑（`G-E00L9PT3V9`），零成本 | 界面难用、隐私差 |

**建议**：留着现有的 Google Analytics（已在收集数据），**额外**加 GoatCounter —— 它能在页面上显示一个低调的访客计数（要"有人来过"的感觉，但不要 ClustrMaps 那个丑地图）。

Loop 结束后我把接入代码写好，你只需去 goatcounter.com 注册拿一个站点名填进去。**到时我会明确告诉你注册链接和填哪里。**

---

## 8. 最终交付物

1. **重构后的主页**（`_layouts/default.html` + `_includes/` + `style.scss`），本地可预览
2. **`design_versions/`** —— 所有版本截图 + 评分记录，可回溯任何一版
3. **`SCORES.md`** —— 每轮改了什么、为什么、得多少分
4. **毒舌评审报告** —— 第三方 agent 的诚实批评及应对
5. **接入说明** —— GoatCounter 怎么配、以后加新论文怎么加
6. **所有改动本地 commit，未 push** —— 你确认满意后再决定上线

---

## 9. 环境已就绪 ✅

工具链已修好并实测通过：

- ✅ **Ruby 3.4 + Jekyll** —— 系统自带 Ruby 2.6 太老跑不动，已切到 Homebrew ruby@3.4，`bundle exec jekyll build` 实测通过
- ✅ **Playwright + Chromium** —— 已安装，实测能对本地站点全页截图
- ✅ **截图管线** —— 已对现有主页截了 baseline，**且已对全部 8 个参考站截图 + 采样背景色**（第 3 节的分析就基于此）

> 跑 Jekyll 需要 `export PATH="/opt/homebrew/opt/ruby@3.4/bin:$PATH"`，已写进脚本，你不用记。

---

## ⬇️ 现在可以开跑

Plan 已完整，方向已修正并经你确认（浅色 + 衬线标题）。剩下的唯一决定：**用哪个模型跑**（建议 Opus 4.8 —— 瓶颈是审美判断和自我批评，不是写 CSS）。
